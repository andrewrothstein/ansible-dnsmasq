---
# handlers file for dnsmasq
- name: disable_systemd-resolved
  become: '{{ dnsmasq_privilege_escalate }}'
  become_user: root
  when: (not dnsmasq_install_pkg_only) and dnsmasq_disable_systemd-resolved
  service:
    name: systemd-resolved
    state: stopped
    enabled: no
